# 系统部署说明书

## 1. 文档用途
- 说明项目如何在新环境中安装、配置、启动和验证。

## 2. 必含内容
- Python 版本与依赖安装。
- MySQL 版本与建库方式。
- `sql/schema.sql` 与 `sql/seed.sql` 的导入顺序。
- `.env` 配置项说明。
- `python run.py` 启动方式。
- 标准测试命令：`python -m pytest -q`。
- 本地验证步骤。

## 3. 建议正文结构
1. 环境依赖
2. 工程获取与安装依赖
3. MySQL 建库与导入 SQL
4. `.env` 配置说明
5. 启动命令
6. 默认账号与演示数据说明
7. 常见问题排查
8. 验证步骤

## 4. 当前状态
- 骨架已创建，待编码完成后填写正式内容。
